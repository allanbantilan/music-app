node_modules/
.expo/

# Native build directories — generated by `expo prebuild` on EAS.
# Keep these out of git so the managed workflow regenerates them fresh.
android/
ios/

dist/
build/
.env
.env.*
*.tsbuildinfo
coverage/
.vite/
